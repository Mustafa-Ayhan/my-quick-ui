import { simplifyEnumAndConst } from './const-enum.js';
export function simplifySingle(node) {
    if (node.type === 'boolean' ||
        node.type === 'integer' ||
        node.type === 'number' ||
        node.type === 'string')
        return simplifyEnumAndConst(node);
    else
        return node;
}
