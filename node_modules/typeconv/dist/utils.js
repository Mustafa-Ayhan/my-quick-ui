export function stringify(value) {
    return JSON.stringify(value, null, 2);
}
export function ensureType(data, typeName, valids, orElse) {
    if (valids.includes(data))
        return true;
    if (data)
        console.error(`Invalid ${typeName}: ${data}`);
    orElse();
}
