import { ILog } from "./ILog";
import { IGuestInfo, ISystemInfo, IUserInfo } from "./IUserInfo";
declare type OmitTyped<T, K extends keyof T> = Pick<T, Exclude<keyof T, K>>;
export interface iuserinfo extends OmitTyped<IUserInfo, "sessionId"> {
    sessionId: {
        inHeader: true;
        decoded: boolean;
    } | string;
}
export interface ILogRequest extends OmitTyped<OmitTyped<ILog, "userInfo">, "correlationId"> {
    correlationId: string | {
        inHeader: true;
    };
    userInfo: iuserinfo | IGuestInfo | ISystemInfo;
}
export {};
//# sourceMappingURL=ILogRequest.d.ts.map