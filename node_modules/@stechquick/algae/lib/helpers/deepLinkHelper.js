"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.DeepLinkHelper = void 0;
var DeepLinkHelper = /** @class */ (function () {
    function DeepLinkHelper() {
    }
    DeepLinkHelper.getMobileOS = function () {
        var userAgent = navigator.userAgent || navigator.vendor || window.opera;
        if (/android/i.test(userAgent)) {
            return "android";
        }
        if (/iPhone/.test(userAgent) && !window.MSStream) {
            return "ios";
        }
        return "unknown";
    };
    DeepLinkHelper.goToLink = function (options) {
        var retVal = this.getMobileOS();
        var opt = options.find(function (i) { return i.platform === retVal; });
        if (!opt) {
            throw new Error("The Platform \"" + retVal + "\" is not implemented yet!");
        }
        window.location.replace(opt.appOpenerUrl);
        setTimeout(function () {
            window.location.replace(opt.appStoreUrl);
        }, 8000);
    };
    return DeepLinkHelper;
}());
exports.DeepLinkHelper = DeepLinkHelper;
//# sourceMappingURL=deepLinkHelper.js.map