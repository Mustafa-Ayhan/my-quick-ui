import { IClientDB, IClientDBOpenOptions, IClientDBConnection, IClientDBVersion, IClientDBInfo } from "./IClientDB";
interface ILogItem {
    level: "debug" | "info" | "log" | "warning" | "error";
    message: string;
    error?: unknown;
    additionals?: Array<any>;
}
interface ILogger {
    log(logItem: ILogItem): void;
}
export declare class ClientDB implements IClientDB {
    private readonly logger;
    constructor(logger: ILogger);
    listDatabases(): Promise<IClientDBInfo[]>;
    openDB(dbName: string, version: IClientDBVersion, options: IClientDBOpenOptions): Promise<IClientDBConnection>;
    deleteDB(dbName: string): void;
}
export {};
//# sourceMappingURL=clientDB.d.ts.map