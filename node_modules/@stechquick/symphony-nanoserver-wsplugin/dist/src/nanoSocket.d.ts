import * as ws from "ws";
import { ISocket, SocketDataType } from "@stechquick/algae/lib/helpers/socket/iSocket";
export interface INanoSocketOptions {
    onMessage?: (message: SocketDataType) => void;
    onClose?: () => void;
}
export declare class NanoSocket implements ISocket {
    private static nsCounter;
    private ws;
    private id;
    private uid;
    private options;
    constructor(ws: ws, options?: INanoSocketOptions);
    set OnMessage(cb: ((message: SocketDataType) => void) | undefined);
    get OnMessage(): ((message: SocketDataType) => void) | undefined;
    set OnClose(cb: (() => void) | undefined);
    get OnClose(): (() => void) | undefined;
    destroy(code?: number, reason?: string): void;
    Send(message: object | string): void;
    Close(code?: number, reason?: string): void;
    get ID(): string;
    get UID(): string;
    private onSocketMessage;
    private onSocketUnexpectedResp;
    private onSocketUpgrade;
    private onSocketPong;
    private onSocketPing;
    private onSocketOpen;
    private onSocketError;
    private onSocketClose;
}
//# sourceMappingURL=nanoSocket.d.ts.map