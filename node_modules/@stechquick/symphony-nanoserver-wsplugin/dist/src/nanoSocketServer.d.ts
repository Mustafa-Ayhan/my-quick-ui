/// <reference types="node" />
import { Server } from "http";
import { INanoServerPlugin } from "@stechquick/symphony-nanoserver/dist/plugin/INanoServerPlugin";
import { NanoSocket } from "./nanoSocket";
interface INanoSocketServerOptions {
    onClose?: () => void;
    onConnection?: (source: NanoSocket) => void;
    onError?: (error: Error) => void;
}
export declare class NanoSocketServer implements INanoServerPlugin {
    PluginName: string;
    private wss?;
    private idIndexedSockets;
    private options;
    private attachPromise?;
    constructor(options?: INanoSocketServerOptions);
    set onClose(cb: () => void);
    set onConnection(cb: (source: NanoSocket) => void);
    set onError(cb: (error: Error) => void);
    Close(): void;
    Attach(server: Server): void;
    GetServerUrl(): string | undefined;
    GetSocket(nanoSocketID: string): NanoSocket | undefined;
    private onSocketConnection;
    private onSocketClose;
    private onSocketError;
    private onSocketListening;
}
export {};
//# sourceMappingURL=nanoSocketServer.d.ts.map