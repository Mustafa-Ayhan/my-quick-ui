import { Command, flags } from '@oclif/command';
export default class UnitTest extends Command {
    static description: string;
    static examples: string[];
    static flags: {
        func: flags.IOptionFlag<string | undefined>;
    };
    run(): Promise<void>;
}
