export declare class Utils {
    static getDateAsString({ date, dateSplitter, timeSplitter, dateAndTimeSplitter }?: {
        date?: Date;
        dateSplitter?: string;
        timeSplitter?: string;
        dateAndTimeSplitter?: string;
    }): string;
}
