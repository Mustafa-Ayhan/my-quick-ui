DİKKAT!!!
Bu dosya aşağıdaki bilgiler doğrultusunda değiştirilmelidir. Şu anki hali patlasın diye kasti olarak yapılmıştır.



1) Eğer module domain komponentiniz yoksa bu dosyaya sadece aşağıdaki satırı <<{{}}>> yazın. Geri kalan herşeyi silin.

{}



2) Eğer domain komponentiniz var ise aşağıdaki örnekte açıklandığı gibi örneği değiştirerek
dosya valid bir json olacak şekilde yazınız:

Açıklamalar: 
  moduleName_componentName : modülünüzün adı_ komponentinizin adı.
  file: komponentinizin dosyası.
  props: komponentinizde kullanılan propertylerinin tipleri ile birlikte listesi.
    type: property'nin tip bilgisi
  events: komponentinizin event'lerinin ve parametrelerinin listesi
    name: event'inizin adı
    desc: editorde gözükmesi istenen açıklama
    params: eğer var ise bu event'in parametrelerinin listesi
      name: parametrenin adı
      type: prop'daki type ile aynı şekilde parametre bilgisi
      desc: parametrenin editorde gözükmesi istenen açıklaması

Örnek: 
{ 
  "moduleName_componentName": {
    "file": "./src/components/domain_label.vue",
    "props": { 
      "propName1": { "type": "string" },
      "propName2": { "type": "{x:number, y: string}" }
    },
    "events": [
      { 
        "name": "event1", "desc": " ", "params": [
          { "name": "param1", "type": "string", "desc": "" },
          { "name": "param2", "type": "string", "desc": "" },
        ] 
      },
      { 
        "name": "event2", "desc": " " 
      }
    ]
  }
 
}

