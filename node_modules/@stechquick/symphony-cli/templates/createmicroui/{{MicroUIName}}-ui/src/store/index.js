// define custom store extensions in Store
// each store module is a nested object of Store
/* Store = {
    myModule: {
        state: { ... },
        mutations: { ... },
        actions: { ... },
        getters: { ... }    
    }
}
*/
const Store = {}

export default Store
