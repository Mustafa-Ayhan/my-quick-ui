default:
  readiness: readiness
  liveness: liveness
  response:
    defaultContentType: "application/json; charset=utf-8"
  test:
    subtestitem1: "test1" 
    subtestitem2: "test2"
  sessionBag:
    environment: none #none | memory | hazelcast | jwt
  bag:
    environment: jwt #memory | hazelcast | jwt 
    hazelcastClusterMembers: [
            "127.0.0.1:5701",
            "127.0.0.2:5701"
        ]
  testKey: ${orange} # -> process.env["orange"]
  test2Key:
    key1: apple # -> apple
    key2: ${apple} # -> process.env["apple"]
    key3: \${apple} # -> ${apple}
    ket4: ${app\}le} # -> process.env["app}le"]
  # stats:
  #   reportUrl: "http://127.0.0.1:4321/"               # [required]
  # #   reportIntervalTime: 300000  #(5min)             # [optional]
  # #   reportIntervalCount: 200                        # [optional]
  # #   observingState:                                 # [optional]
  # #       samplingFrequency: 10                       # [optional]
  # #       failureStateThreshold: 3                    # [optional]
  # #       failureStateIntervalTime: 180000  #(3min)   # [optional]