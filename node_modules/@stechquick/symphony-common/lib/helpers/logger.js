"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    Object.defineProperty(o, k2, { enumerable: true, get: function() { return m[k]; } });
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.Logger = void 0;
const ILogItem_1 = require("../interfaces/ILogItem");
const fs = __importStar(require("fs"));
class Logger {
    static log(logItem) {
        const params = [];
        if (logItem.timeFormat) {
            params.push(this.getDatetimeString(logItem.timeFormat));
        }
        params.push(logItem.message);
        if (logItem.error) {
            params.push(logItem.error);
        }
        if (logItem.additional) {
            params.push(logItem.additional);
        }
        const targetLog = this.getTargetLog(logItem.level);
        targetLog.apply(console, params);
        if (logItem.diskPermission) {
            this.writeTxtLog(params, logItem.logDiskPath);
        }
    }
    static getDatetimeString(format) {
        const date = new Date();
        switch (format) {
            case "date":
                return date.toLocaleDateString('tr-TR');
            case "datepath":
                return `${date.toISOString().slice(0, 10)}`;
            case "time":
                return `${date.toLocaleTimeString('tr-TR')}.${date.getMilliseconds()}`;
            case "datetime":
                return `${date.toLocaleString('tr-TR')}.${date.getMilliseconds()}`;
        }
    }
    static getTargetLog(logLevel) {
        let targetLog;
        switch (logLevel) {
            case ILogItem_1.LogLevel.debug:
                targetLog = console.debug;
                break;
            case ILogItem_1.LogLevel.info:
                targetLog = console.info;
                break;
            case ILogItem_1.LogLevel.log:
                targetLog = console.log;
                break;
            case ILogItem_1.LogLevel.trace:
                targetLog = console.trace;
                break;
            case ILogItem_1.LogLevel.warn:
                targetLog = console.warn;
                break;
            case ILogItem_1.LogLevel.error:
                targetLog = console.error;
                break;
            default:
                targetLog = console.log;
        }
        return targetLog;
    }
    static writeTxtLog(params, path) {
        fs.writeFileSync(`${path}/symphonyLog-${this.getDatetimeString("datepath")}.txt`, params + "\r\n", { encoding: 'utf8', flag: 'a+' });
    }
}
exports.Logger = Logger;
//# sourceMappingURL=logger.js.map