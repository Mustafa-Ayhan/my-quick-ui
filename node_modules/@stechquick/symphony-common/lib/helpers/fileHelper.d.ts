import { IFileInfo } from "../interfaces/IFileNetworkMessage";
export interface IExportFileSettings {
    sourcePath: string;
    targetPath: string;
    replaceMap: Map<RegExp, string>;
}
export declare enum ItemType {
    Directory = 0,
    File = 1,
    FIFO = 2,
    Socket = 3,
    SymbolicLink = 4
}
export declare class FileHelper {
    private static slashCache?;
    private static discardReplacementExt;
    static get Slash(): string;
    static removeDist(): void;
    static removeFolder(path: string): void;
    private static deleteFolderRecursive;
    private static exportFilesInner;
    static exportFiles(settings: IExportFileSettings): void;
    static getFileNamesFormPath({ selectedPath, excludeFolders, selectedExtensions }: {
        selectedPath: string;
        excludeFolders?: boolean;
        selectedExtensions?: string[];
    }): IFileInfo[];
    static isDirectory(itemPath: string): boolean;
}
//# sourceMappingURL=fileHelper.d.ts.map